# 奇物局第二轮迭代

用户批准按头脑风暴的最终取舍执行：先分别验证怪机器与罢工谈判，大撤离保留为后续竞争方向。本文是执行计划；玩法约束在 `.nimi/spec/playground.authority.yaml`，运行与路由继续由 Nimi SDK/Kit 的既有合同约束。

1. **照片游乐场入口**：沿用现有视觉，选择照片与活动；共用真实定位与 Nimi 能力设置。保留现有探案入口。新活动的照片、规则、承诺和结果通过 App storage 保存。
2. **怪机器**：AI 为本张照片生成器件性格与合法转换规则。玩家在照片上点选/连线，运行前可看规则与线路代价；运行后看到逐站音符变化、听到合成结果。可拆接、倒出、清空并继续试验。目标必须存在可行线路，成功后仍可自由尝试。同一局不再由 AI 临时决定运行结果。
3. **罢工谈判**：AI 为本张照片生成角色诉求和有解的工作安排。玩家有一个休假名额和一个署名名额，需要落实写故事、朗读和布置现场。自然语言先转为可审阅的提议，确认才执行；直接操作和文字提议走同一组规则。角色承诺、条件与资源公开显示，不能事后反悔。安排完成后生成并演出微型故事，可选 Nimi 语音。
4. **真实验收与交付**：先跑受影响规则测试及构建；从官方 `pnpm dev` 的监督 Electron 运行真实照片定位、两个活动的 AI 生成、玩家操作、结果及恢复。合成数据仅用于规则测试。记录未运行的路径，不用 UI 预填数据冒充生成。最后更新 PLAY、PRODUCT 和衍生设计文档，并提交本地 checkpoint。

验收关注玩家行为：怪机器完成目标后仍能主动换线比较；罢工中的确认确实消耗名额、限制后续选择并改变最后的演出。两种活动分别呈现，不叠加附身、重拍或时间循环。

执行状态：四步已实现，真实 CDP 验收与独立界面审阅已完成。

本轮实际结果：

- 怪机器用真实照片节点完成点选与拖拽连线。不同线路得到不同声音；预算不足时不算挑战成功，换线后成功。成功后可继续比较、存放和倒出声音，重开恢复。
- 罢工谈判用真实 AI 生成角色诉求，将自然语言转成可确认的五项安排；确认前不写入承诺。三幕童话、可选 Nimi 语音、续幕恢复和谢幕已运行。
- 另一场真实谈判验证了不可完成的选择：事前预警、确认散场、承诺不撤回、重开恢复、回到入口再谈。取消新生成保留旧局。
- 18项规则测试、scaffold check、完整构建与5个 authority 单元检查通过。更多用户照片、真实手机触控和生产包路径为 `NOT-VERIFIED`，不以本轮试玩照片和窄屏模拟代替。
- 独立界面审阅发现窄屏器件规则距离点选区过远；已将完整规则移至物品条之后、线路之前，并在选物后带入视口。CDP连续切换物品与运行线路通过，审阅者将该修复评为 resolved、disposition 为 ship。该结论限于这项修复。

桌面大撤离、附身、时间循环与重拍任务继续留在候选方向，不混入本轮两个活动。
