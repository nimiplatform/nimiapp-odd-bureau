# 奇物局

把一张普通照片变成可以连线、谈判、演出和探案的奇物游乐场。

照片中的物品由 Nimi `vision.locate` 实时定位，成为器件或角色。当前主入口提供怪机器与罢工谈判；右上角「照片探案」保留原有的调查、盘问与指认玩法。同一张照片可生成不同的局面。

AI 在开局创作固定的器件规则或角色诉求；玩家的操作由这些规则执行。所有故事与物品的拟人化本领都是虚构游戏。

## 运行

本 App 已通过 app-tools 的正式 `create → install → init → sync → check` 流程创建。SDK、Kit 和 app-tools 均使用公开 npm 版本，无 workspace 依赖替换。

先打开 Nimi 并启用本地 App 开发，然后在本目录执行：

```powershell
pnpm install --frozen-lockfile
pnpm dev
```

这是独立仓库，使用自己的 pnpm workspace 和锁文件。App 由 Desktop 监督的 Electron Host 启动，不能直接打开 Vite 页面代替真实运行。

第一次打开，在「能力设置」为「看见物品」和「创作玩法」配置能力；可以使用 Kit 提供的「使用本机当前模型」按钮。定位能力使用本机环境。请同时确保 Nimi 的 Runtime → 环境中，定位所需组件已安装。模型已选择不代表运行环境已经准备好。

## 怪机器

选择照片并生成机器，点发声物品作为起点，再接改造器和容器；也可以从一个物品拖到另一个物品。物品规则、运行预览和走线预算都能先查看。点击启动，声音逐站变换并装进容器。

目标要求把指定旋律送入指定容器，并满足本局的走线预算。达成目标后仍能拆接试验。移除一个节点会连接其前后节点；再次点线路中的节点会拆掉它后面的部分。容器最多保留32个音，「倒出来听」完整播放后清空；停止播放会保留容器内容。

机器声音是固定音符规则的本地演奏，不是伪造的 Runtime 音频任务。它无需语音模型。AI 负责创作这张照片的器件计划，已经确定的转换结果不会由 AI 临时改判。

## 罢工谈判

故事会需要写故事、朗读、布置现场三份工作，有一个休假名额和一个署名名额。休假伙伴不能工作，署名须给工作者，每件物品最多承担两份工作。

点物品查看它愿意承担的工作及条件。可以直接提议给名额或分配工作，也可以用一句自然语言描述多个安排。「整理提议」只生成待审阅的动作，点击确认才写入承诺板。已经许出的名额与工作不可在本局撤回。

如果一份提议会让剩余工作无法安排齐，确认前会明确提示。仍然接受，就以「未能开场」结束这一局；承诺保留，可以另开一轮尝试不同安排。

安排齐全后，点击开场，由 Nimi 创作三幕微型童话；逐幕阅读并谢幕。可选「听这一幕」调用 Nimi 语音合成，声音从 Runtime 的中文声音目录选择。语音失败仍可阅读，故事生成失败仍保留已经谈好的安排。

## 原有照片探案

1. 直接使用「早餐之后」试玩照片，或上传 JPG、PNG、WebP 照片。建议有至少三件分开摆放的物品。
2. 选择故事风格，开案。
3. 点击照片里的物品。也可用照片下方的物品按钮通过键盘探索。
4. 问关键证词、自由盘问，在「线索本」比较不同角色的说法。
5. 点击「我知道是谁了」，在照片中指认并确认，揭晓本案。
6. 选择同一张照片再来一案，或使用新照片。

照片在处理后最长边不超过 1600 像素。建议使用杯子、书、灯、时钟等种类不同、分开摆放的物品。当前游乐场的照片、规则、线路、声音序列或承诺通过 SDK 的 App storage 保存；原有探案保留自己的当前案卷。游乐场一次保留一局。取消新生成不会覆盖已有局面。同一张照片的已验证定位会被复用。

## 代码与检查

- `src/odd-bureau/App.tsx`：照片现场、探索、角色对话、线索本、指认与结局。
- `src/odd-bureau/Playground.tsx`：活动入口、准备、当前局保存与恢复。
- `src/odd-bureau/play-rules.ts`：固定机器规则、可达目标、承诺与条件。
- `src/odd-bureau/MachineGame.tsx`、`StrikeGame.tsx`：两个活动的照片操作界面。
- `src/odd-bureau/play-runtime.ts`：真实 AI 计划、自然语言提议、演出与保存。
- `src/odd-bureau/nimi.ts`：公开 SDK/Kit 的定位、文本、语音与 App storage。
- `src/odd-bureau/game.ts`：真实坐标命中、角色与案件约束、生成提示。
- `src/odd-bureau/Setup.tsx`：Kit 的 App AIConfig 配置界面。
- `src/shell/routes/product-area.tsx`：接入 scaffold 的产品入口。

```powershell
pnpm check
pnpm test
pnpm build
```

规则测试覆盖坐标、案件引用、机器变换与容量，以及承诺条件和可行安排；合成测试数据不作为真实 AI 验收。App 无预写谜案、预制成功计划或硬编码热点作为失败时的替代。

## 本次移交

2026-09-12 起以本独立仓库为开发入口。app-tools 的正式初始化状态已保留，目录记录已对齐新仓库，并重新执行了 `sync` 和 `check`。旧 Nimi 仓库的活动 App 目录已移至其 `.nimi/local/odd-bureau-source-archive` 本地归档。

本仓库已完成冻结依赖安装、`check`、规则测试、构建与 Desktop 监督的真实 Electron 启动。实际使用试玩照片生成了「下午茶失踪案」，定位六件物品、收集六份证词、重开恢复进度、指认杯子并揭晓结局均已运行。图片导入后的来源标签和替代文本也已检查。迁移前还验证了自由盘问、键盘选择和通过 Runtime 目录选择中文声音后的语音播放；这些产品代码完整保留。

当前已验证的是 Windows 本地开发体验；生产安装包、上架与发布为 `NOT-VERIFIED`。后续可在此仓库继续打磨不同照片的故事质量、难度与节奏。

## 第二轮玩法验收

两个新活动均在本仓库通过官方开发命令启动，以 CDP 连接该 App 的 Electron renderer 验收。试玩照片的定位、器件与诉求计划、自然语言提议和童话均由真实 Nimi 调用产生。

- 怪机器：实际拖拽连线，比较不同顺序的音符与线长；旋律正确但超预算仍未过关，换线后达成目标。成功后继续改线、累积声音、重开恢复，再完整播放并倒空容器。
- 罢工谈判：未满足署名条件的工作不能确认；一句话生成五项安排，预览不改承诺，确认才落实。三幕童话、真实语音播放至结束、重开续幕和谢幕均已运行。
- 失败与取消：真实安排触发散场预警，确认后保存两项不可撤回的承诺；重开恢复失败结局。取消新局后可继续原有故事会，再次开局成功。

规则测试覆盖18项行为，`pnpm check`、`pnpm test`、`pnpm build` 和本仓库玩法 authority 检查通过。窄屏为 Electron 内的 CDP 视口模拟；手机安装与触控设备、更多用户照片上的两个新活动、生产安装包及发布为 `NOT-VERIFIED`。

## 素材来源

`public/breakfast-scene.png` 是使用内置 Imagegen 生成的虚构试玩场景，界面明确标注。它只是一张输入照片，没有预计算的角色、热点或答案。生成提示已嵌入 PNG 元数据。

字体：ZCOOL QingKe HuangYou，通过 `@fontsource/zcool-qingke-huangyou` 自托管；许可随该 npm 包提供。图标来自 Lucide；常规控件与宿主桥接来自 Nimi Kit。
